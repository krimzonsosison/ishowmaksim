using UnityEngine;

public class zds : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {

        for (int i = 5; i <= 16; i++)
        {
            print(i);
        }
        for (int i = 68; i <= 98; i++)
        {
            print(i);
        }
        for (int i = -4; i <= 10; i++)
        {
            print(i);
        }
                
        for (int i = 10; i >= 0; i--)
        {
            print(i);
        }
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
