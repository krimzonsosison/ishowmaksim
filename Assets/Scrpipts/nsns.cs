using UnityEditor.Media;
using UnityEngine;

public class nsns : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        int h1 = 10;
        int m1 = 30;
        int h2 = 1;
        int m2 = 20;
        h1 = h1 + h2;
        m1 = m1 + m2;


    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
